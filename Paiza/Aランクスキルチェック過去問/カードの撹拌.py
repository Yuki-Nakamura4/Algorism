# 実行時間制限でアウト
# N = int(input())
# A = [input() for _ in range(2* N)]

# ans = 0

# for i in range(1, N+1):
#     index = [j for j , x in enumerate(A) if x == i]
#     distance = index[1] - index[0] - 1
#     ans += distance

# print(ans)